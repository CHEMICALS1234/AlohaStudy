#include <iostream>
#include <queue>
#include <vector>

using namespace std;

int main()
{
  int n, k, l;
  cin >> n;
  cin >> k;
  int apple[k][2];
  for (int i = 0; i < k; i++)
    cin >> apple[i][0] >> apple[i][1];
  cin >> l;
  vector<pair<int, char>> q;
  return 0;
}