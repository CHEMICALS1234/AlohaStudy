#include <algorithm>
#include <iostream>

using namespace std;

int TC, N, M, W;

int main() {
  scanf("%d", &TC);
  for (int i = 0; i < TC; i++) {
    scanf("%d %d %d", &N, &M, &W);
  }
  return 0;
}